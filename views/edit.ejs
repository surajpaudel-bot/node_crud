<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>

    <!-- Bootstrap CSS for responsive design and pre-styled components -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- FontAwesome Icons for adding iconography like edit, save, etc. -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">

    <style>
        /* General body styles for background and font */
        body {
            background-color: #faf7f2;
            color: white;
            font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
        }

        /* Container to center the content with a max width */
        .container {
            max-width: 900px;
            margin-top: 40px;
        }

        /* Form container styles - background, padding, border, shadow */
        .form-container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #e0c097;
        }

        /* Title inside the form container */
        .form-container h2 {
            margin-bottom: 20px;
            color: #5a3e2b;
        }

        /* Styles for form fields */
        .form-control, textarea {
            margin-bottom: 15px;
            border-radius: 8px;
        }

        /* Focused state for form fields to enhance accessibility */
        .form-control:focus, textarea:focus {
            box-shadow: 0 0 0 0.25rem rgba(38, 143, 255, 0.5);
        }

        /* Label styles inside the form */
        form label {
            color: #5a3e2b;
        }

        /* Primary button styles - background color, size, and hover effect */
        .btn-primary {
            width: 100%;
            padding: 12px;
            background-color: #5a3e2b;
            border: none;
            font-size: 16px;
            border-radius: 8px;
        }

        /* Button hover effect */
        .btn-primary:hover {
            background-color: #3e2b1e;
        }

        /* Navbar styles - background color and spacing */
        .navbar {
            margin-bottom: 30px;
            background-color: #5a3e2b;
        }

        /* Navbar link styles - font size, color, and hover effect */
        .nav-link {
            font-size: 16px;
            color: white;
        }

        .nav-link:hover {
            color: #3e2b1e !important;
        }

        /* Navbar brand styling for title */
        .navbar-brand {
            color: white;
            font-size: 32px;
            letter-spacing: 4px;
        }

        /* Footer styling - background color and text alignment */
        footer {
            background-color: #5a3e2b;
            color: white;
            padding: 20px;
            text-align: center;
        }

        /* Footer links styling */
        footer a {
            color: #e0c097;
        }

        /* Footer link hover effect */
        footer a:hover {
            color: #fff;
        }
    </style>
</head>
<body>

    <!-- Bootstrap Navbar: Contains site links and brand name -->
    <nav class="navbar navbar-expand-lg navbar-light shadow-sm">
        <div class="container">
            <!-- Brand Name -->
            <a class="navbar-brand" href="#">Purano माल</a>

            <!-- Navbar Toggle Button for mobile responsiveness -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar Links -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <!-- Home Link -->
                    <li class="nav-item">
                        <a class="nav-link" href="./create">Home</a>
                    </li>
                    <!-- About Link -->
                    <li class="nav-item">
                        <a class="nav-link" href="#">About</a>
                    </li>
                    <!-- Contact Link -->
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content Area -->
    <div class="container">
        <div class="form-container">
            <!-- Form Heading -->
            <h2>Edit Vintage Item <i class="fas fa-edit"></i></h2>

            <!-- Form for Editing Item Details -->
            <form action="/update/<%= item._id %>" method="post">
                <!-- Item Name Input Field -->
                <div class="mb-3">
                    <label for="item_name" class="form-label">Item Name</label>
                    <input type="text" name="item_name" id="item_name" class="form-control" value="<%= item.item_name %>" required>
                </div>

                <!-- Price Input Field -->
                <div class="mb-3">
                    <label for="price" class="form-label">Price</label>
                    <input type="number" name="item_price" id="price" class="form-control" value="<%= item.item_price %>" required>
                </div>

                <!-- Status Input Field -->
                <div class="mb-3">
                    <label for="status" class="form-label">Status</label>
                    <input type="text" name="item_status" id="status" class="form-control" value="<%= item.item_status %>" required>
                </div>

                <!-- Image URL Input Field -->
                <div class="mb-3">
                    <label for="image_url" class="form-label">Image URL</label>
                    <input type="text" name="image_url" value="<%= item.image_url %>" required class="form-control" />
                </div>

                <!-- Description Textarea -->
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea name="item_description" id="description" class="form-control" rows="4"><%= item.item_description %></textarea>
                </div>

                <!-- Submit Button to Update Item -->
                <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Update Item</button>
            </form>
        </div>
    </div>

    <!-- Footer Section -->
    <footer>
        <p>&copy; 2024 Purano माल | All rights reserved.</p>
        <a href="#">Privacy Policy</a> | <a href="#">Terms of Service</a>
    </footer>

    <!-- Bootstrap JS and Dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
